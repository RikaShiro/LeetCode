/**
 * @param {number[]} prices
 * @return {number}
 */
var maxProfit = function (prices) {
	let [dp0, min] = [0, prices[0]]
  const n = prices.length
}

const prices = [6, 1, 3, 2, 4, 7]
console.log(maxProfit(prices))